import XCTest
@testable import PetzPack

final class PetzPackTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(PetzPack().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
