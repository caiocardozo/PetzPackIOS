struct PetzPack {
    var text = "Hello, World!"
}

struct Calculator {
    public init() {}
    
    public static func multply(_ valueX: Int, _ ValueY: Int) -> Int {
        return valueX * ValueY
    }
}
